# prompt-codec v0.3 Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development to implement this plan. Steps use checkbox (`- [ ]`) syntax.

**Goal:** Persistent rewrite cache (SQLite tier), warm-model keep-alive pinner, cumulative savings telemetry with upstream cached-token capture.

**Spec:** `docs/superpowers/specs/2026-07-18-v0.3-design.md` is the behavior authority — read it in full first. Baseline: v0.2 on branch `v0.3` (92 tests green).

**Conventions:** TDD per task; before every commit run `cargo test` (verify exit code, don't pipe it away), `cargo clippy --all-targets -- -D warnings`, `cargo fmt --check`. Commit trailer: `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`.

---

### Task 1: persistent cache tier

**Files:** Modify `src/cache.rs`, `src/config.rs` (CacheConfig + KNOWN_SECTIONS), `Cargo.toml` (`cargo add rusqlite --features bundled`), `src/proxy.rs`/`src/main.rs` only if health wiring requires; new integration test in `tests/codec_test.rs`.

Steps:
- [ ] Failing unit tests in `src/cache.rs` (use `tempfile` dirs): `disk_roundtrip_survives_new_instance` (put via one `RewriteCache`, get via a NEW instance on the same path); `disk_miss_returns_none`; `prune_caps_disk_entries` (cap 8, insert 20 with distinct `last_used`, force prune via the counter, assert count ≤ 8 and newest survive); `corrupt_db_degrades_to_memory` (write garbage bytes to the path first — construction succeeds, put/get work memory-only); `persist_false_writes_nothing` (no file created).
- [ ] Failing integration test: `disk_cache_survives_codec_rebuild` — wiremock LLM `expect(1)`; Codec A (cfg with tempdir cache path) encodes a long message via LLM; drop A; Codec B (same path) encodes the same message → cache hit, output identical, mock still at 1 call.
- [ ] Implement per spec §Feature 1: `CacheConfig { max_entries, persist: bool, path: Option<PathBuf>, max_disk_entries: u64 }` (defaults true/None/100_000; `None` path → `dirs::cache_dir()/prompt-codec/rewrites.sqlite3`, create parent dirs); `RewriteCache::new_with_disk(mem_entries, disk: Option<DiskCacheConfig>)` keeping the existing `new()` for tests; disk tier `Arc<Mutex<rusqlite::Connection>>`, WAL, busy_timeout 250, schema + meta table with `schema_version=1`; read-through promote + `last_used` touch; write-through; probabilistic prune with the `key IN (SELECT ...)` form; all disk errors → single-warn degrade. Expose `disk_entry_count(&self) -> Option<u64>` and `meta_get/meta_set(&self, k, v)` (Feature 3 needs them).
- [ ] Wire `Codec::new` to build the disk tier from config; `/health` adds `cache_disk_entries` + `cache_path`; update the existing health integration test expectations.
- [ ] Update `config.example.yaml` + README config table (three new keys).
- [ ] Gates; commit `feat: persistent SQLite tier for the rewrite cache`.

### Task 2: keep-alive pinner

**Files:** Modify `src/llm.rs` (pin fn), `src/main.rs` (spawn in proxy arm), `src/config.rs` (LocalConfig.keep_alive + KNOWN_SECTIONS), `tests/llm_test.rs`.

Steps:
- [ ] Failing tests (`tests/llm_test.rs`, wiremock): `pin_posts_native_generate` — mock `POST /api/generate` asserting body `{"model": <default>, "prompt": "", "keep_alive": "60m"}`, `pin_model(...)` returns Ok; `pin_stops_after_unsupported` — 404 mock, first call returns Err(Unsupported-ish), caller contract documented (the loop in main stops on first error).
- [ ] Implement `LlmClient::pin_model(&self, keep_alive: &str) -> anyhow::Result<()>`: POST `{base_url stripped of trailing /v1}/api/generate`, 120 s per-request timeout override, treat any non-2xx or transport error as Err. `LocalConfig.keep_alive: String` default `"60m"`.
- [ ] `src/main.rs` proxy arm: after the existing startup probe, if `keep_alive` non-empty AND mode is local/hybrid, `tokio::spawn` a loop: pin immediately, then every 20 min; on first Err, `tracing::warn!("keep-alive pinning disabled: ...")` once and end the task. Must not delay `axum::serve`.
- [ ] `/health` local section includes `keep_alive` when pinning is configured on (plumb via existing config in AppState — display only).
- [ ] Update `config.example.yaml` + README table. Gates; commit `feat: warm-model keep-alive pinner (proxy, local/hybrid only)`.

### Task 3: savings telemetry

**Files:** New `src/telemetry.rs` (+ `pub mod telemetry;`), modify `src/proxy.rs` (counters in AppState, tail inspector around the two compressing endpoints' response streams, /health totals), `src/cache.rs` meta usage, `tests/proxy_test.rs`.

Steps:
- [ ] Failing unit tests in `src/telemetry.rs`: `TailBuffer` keeps only last 16 KB across many pushes; `extract_cached_tokens` finds `"cached_tokens":123` in (a) a plain JSON body tail, (b) a final SSE `data:` chunk, (c) split across two pushes straddling the ring boundary (push byte-by-byte in the test); returns None when absent.
- [ ] Failing integration tests (`tests/proxy_test.rs`): `health_totals_accumulate` — two chat requests through wiremock upstream whose response includes `"usage":{"prompt_tokens":100,"prompt_tokens_details":{"cached_tokens":40}}` → /health totals: requests 2, saved_tokens > 0, upstream_cached_tokens 80, responses_with_cache_info 2; `totals_survive_restart` — spawn proxy A with tempdir cache path, send request, read totals; spawn proxy B same path → totals carried over (requests ≥ 1 before new traffic); existing SSE byte-identity + causality tests must still pass UNCHANGED (they are the inspector's no-distortion guard).
- [ ] Implement: `Totals` (AtomicU64 fields + `since`), stored in `Arc` in AppState; increment in chat/completions handlers only when stats were produced; `TailBuffer` (fixed 16 KB ring) + `extract_cached_tokens(tail: &[u8]) -> Option<u64>` via regex on the lossy-utf8 tail; wrap ONLY chat/completions response streams in an inspector stream that pushes chunks to the ring and on stream end runs extraction + counter updates (catch-all untouched); /health `totals` object per spec; persistence via cache `meta_get/meta_set("totals_json")` — load at create_app, flush every 32 requests and in the graceful-shutdown path.
- [ ] Update README (totals in health, best-effort cached-token caveat). Gates; commit `feat: cumulative savings telemetry with upstream cached-token capture`.

### Task 4: verification & cutover

- [ ] Full gates with explicit exit codes; version → 0.3.0 in Cargo.toml.
- [ ] Live acceptance (spec §Acceptance): CLI cross-process cache hit (criterion 2, real qwen); warm-latency spot check if Ollama is up (criterion 3 — pin, wait, measure one hybrid call < 3 s); /health totals across a real proxy restart (criterion 4); non-Ollama pin behavior via wiremock (criterion 5, covered by tests — cite).
- [ ] Record results in this file under "## Task 4 results"; commit `chore: v0.3.0 — persistent cache, warm model, savings telemetry`.
